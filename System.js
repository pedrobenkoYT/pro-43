class System{

    constructor(){}

    authenticate(REVBAILA,VARIABLE){
        textSize(50);
        fill("black");
        text(code,300,300)};
        //adicionar código para return true se a resposta correta for inserida.
        authenticate(LIREOGO,GOLEIRO){
            textSize(50);
            fill("black");
            text(code,300,300)
    }
    authenticate(BUURU,URUBU){
        textSize(50);
        fill("black");
        text(code,300,300)
}
    if (actualCode === enteredCode.toUpperCase())

      return true

    else

      return false
   i

}
